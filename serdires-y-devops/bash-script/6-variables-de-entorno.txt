El usuario ( o un script ) puede actualizar y añadir variables, exportando sus valores al entorno del intérprete (comando export),
loque afectará también a todos los procesos hijos generados por él. El administrador puede definir variables de entorno estáticas
para los usuarios del sistema.

Para listar las variables de entorno:

    env 

