// OPERADORES UNARIOS

// Incremento

let incremento = 10
incremento++
console.log(incremento)

// Igual con el decremento --

// OPERADORES TERNARIOS

// Su funcionamiento funciona a razón de una terna (tres elementos). Este evalúa una condicion. De ser verdadero, procederá con el segundo término
// de esta operación, si no con el tercero.

let a = 5, b = 8

let c = b > a ? "Es verdad" : "Es falso"